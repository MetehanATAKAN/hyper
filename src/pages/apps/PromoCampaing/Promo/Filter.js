import React from 'react';
import Filter from '../../../../components/Filter';

const Filters = ({ filterComponentsData }) => {
    return <Filter filterComponentsData={filterComponentsData} isFilterBtn={false} />;
};

export default Filters;
